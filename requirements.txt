// Requirements Local Agentic RAG
python3 -m pip install langchain-nomic langchain_community tiktoken langchainhub
python3 -m pip install ollama
python3 -m pip install jupyter
python3 -m pip install chromadb
python3 -m pip install langchain-chroma
python3 -m pip install langchain-ollama
python3 -m pip install -U langchain-google-community  
python3 -m pip install -U langgraph
python3 -m pip install -U langchain
python3 -m pip install "fastapi[standard]"

// Requirements Cloud Adaptative RAG
python3 -m pip install "uvicorn[standard]" # Command to run: uvicorn main:app --reload
python3 -m pip install -U langchain_community tiktoken langchain-openai langchain-cohere langchainhub langchain-chroma langchain langgraph pydantic typing-extensions langchain-google-community langchain-core  
python3 -m pip install beautifulsoup4
